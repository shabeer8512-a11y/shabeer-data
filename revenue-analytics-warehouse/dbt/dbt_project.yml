name: revenue_analytics_warehouse
version: 1.0.0
config-version: 2

profile: revenue_analytics_warehouse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  revenue_analytics_warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table
